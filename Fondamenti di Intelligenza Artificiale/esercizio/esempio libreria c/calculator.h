/*
 * FILE: calculator.h
 * SCOPO: Header della libreria calculator
 *
 * Questo file contiene le DICHIARAZIONI (prototipi) delle funzioni
 * Non contiene il codice vero e proprio, solo le "firme"
 */

#ifndef CALCULATOR_H
#define CALCULATOR_H

/* Somma due numeri */
int somma(int a, int b);

/* Sottrae b da a */
int sottrazione(int a, int b);

/* Moltiplica due numeri */
int moltiplica(int a, int b);

/* Divide a per b (restituisce 0 se b è zero) */
int dividi(int a, int b);

#endif
